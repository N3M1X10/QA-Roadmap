# bug_reports.md

## Пример баг-репорта 🐛

### Описание (Summary)
Кнопка **отправки сообщения** в чате неактивна при вводе текста.

### Шаги воспроизведения (Steps to Reproduce)
1. Открыть главное окно чата.
2. Кликнуть в поле ввода сообщения.
3. Ввести текст "Привет".
4. Обратить внимание на кнопку "Отправить".

### Фактический результат (Actual Result)
Кнопка "Отправить" остаётся серой и не нажимается. Текст отправить невозможно.

### Ожидаемый результат (Expected Result)
После ввода хотя бы одного символа кнопка "Отправить" становится активной (меняет цвет) и отправляет сообщение при клике.

---

## Жизненный цикл бага (Bug Lifecycle)

Основные статусы, которые проходит ошибка:
* **New (Новый)** — баг обнаружен тестировщиком и внесён в систему.
* **In Progress (В работе)** — разработчик взял баг на исправление.
* **Fixed (Исправлен)** — код починен, баг отправлен обратно на проверку к QA.
* **Closed (Закрыт)** — QA перепроверил исправление, баг больше не воспроизводится.

* **Rejected (Отклонён)** — Баг-репорт отклонён, по какой-либо причине.